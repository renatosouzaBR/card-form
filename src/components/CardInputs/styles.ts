import { styled } from "@/styles/stitches.config"

export const CardInputsContainer = styled('div', {
  display: 'flex',
  flexDirection: 'column',
  gap: 24,
  maxWidth: 328,
  width: '100%',
})

export const CardInputsGroupRow = styled('div', {
  display: 'flex',
  gap: 16,
})